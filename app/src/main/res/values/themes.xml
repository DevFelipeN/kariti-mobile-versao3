<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kariti" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
<!--         <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Kariti" parent="Base.Theme.Kariti" />

    <style name="BACKGROUND">
        <item name="android:background">@android:color/background_light</item>
    </style>

    <style name="BUTTONSTYLE" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="BUTTONSTYLEdark" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_select_dark</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="COLORTEXT">
        <item name="android:textColor">@android:color/background_dark</item>
    </style>

    <style name="STATURBARTHEME" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Outras configurações do tema -->
        <item name="android:statusBarColor">@android:color/holo_purple</item>
    </style>

</resources>